import basePrettierConfig from "../base-prettier.config.mjs";

/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
	...basePrettierConfig
};

export default config;
